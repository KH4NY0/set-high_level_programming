# Python - Inheritance

Subclassing built-in and custom types, overriding methods, `super()`, and the
difference between `type()`, `isinstance()` and `issubclass()`.

## Files

| File | Description |
| --- | --- |
| `0-lookup.py` | `lookup()` - returns the attributes and methods of an object |
| `1-my_list.py` | `MyList` - a `list` subclass with `print_sorted()` |
| `2-is_same_class.py` | `is_same_class()` - exact type match only |
| `3-is_kind_of_class.py` | `is_kind_of_class()` - type match or subclass |
| `4-inherits_from.py` | `inherits_from()` - strict subclass only |
| `5-base_geometry.py` | An empty `BaseGeometry` class |
| `6-base_geometry.py` | Adds an `area()` that raises |
| `7-base_geometry.py` | Adds `integer_validator()` |
| `8-rectangle.py` | `Rectangle`, inheriting the validator |
| `9-rectangle.py` | Adds `area()` and `__str__` to `Rectangle` |
| `10-square.py` | `Square`, inheriting `Rectangle`'s description |
| `11-square.py` | `Square` with its own `__str__` |
| `100-my_int.py` | `MyInt` - an `int` with `==` and `!=` inverted |
| `101-add_attribute.py` | `add_attribute()` - no `try`/`except` allowed |
| `tests/1-my_list.txt` | Doctests for `MyList` |
| `tests/7-base_geometry.txt` | Doctests for `BaseGeometry` |
